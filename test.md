BÁO CÁO KIỂM TOÁN — Principal Architect & QA (READ-ONLY, không sửa file nào)
Phạm vi đã đọc trực tiếp: ai/agent.py, ai/tools.py, ai/schemas.py, ai/auditor.py, ai/worker.py, data/incident_store.py, data/pipeline.py, data/wap.py, data/dq.py, data/incidents.py, web/backend/api.py, web/backend/security.py, web/frontend/ui.py, web/frontend/rendering.py, web/frontend/static/index.html, data/dbt/*, ai/runbooks/*, config.py. Lưu ý: index.html đã bị ai đó thêm hàm debug (forceLoadChainlit, testNavigation ~dòng 1286+) sau lần audit trước — báo cáo dưới đây đối chiếu với trạng thái file hiện tại.

PHẦN 1: BẢNG ĐỐI CHIẾU FLOW (GAP ANALYSIS)
Bước Quy Trình	Hiện Trạng Code (file & dòng)	Đánh Giá	Chi Tiết Khoảng Trống (Gap)
1. Pipeline & Flag Table	data/pipeline.py:461-468 job fail → open_incident (status DETECTED) + notification; data/incident_store.py:212-281 ghi bảng incidents + job_runs; ai/worker.py:168-186 process_pending poll incident DETECTED rồi điều tra	ĐẠT (có lưu ý)	Agent 1 không tự đọc bảng flag job (job_runs) mà nhận việc theo cơ chế push: pipeline ghi incident → worker poll incidents. Trong lúc điều tra Agent có đọc dq_test_results (ai/agent.py:132). Đủ dùng, nhưng nếu BGK hỏi "agent tự phát hiện job xong bằng flag nào" thì câu trả lời đúng là incident row, không phải job flag.
2. Ghi bảng Temp (Zero Blast Radius) — luồng chính	data/wap.py:251 assert_shadow_only, wap.py:315-405 preflight EXPLAIN+dry-run+ROLLBACK, wap.py:618-750 stage_remediation chỉ COMMIT shadow/quarantine; ai/tools.py:537 đòi giấy phép PHASE_STAGE; tool_atomic_publish_to_prod đòi PHASE_PUBLISH (tools.py:590), grant chỉ cấp trong publish_prod (api.py:541-547) rồi thu hồi ngay	ĐẠT cho luồng WAP chuẩn	Bảng thật không bị chạm ở bước vá trên luồng chuẩn. Tuy nhiên tồn tại 3 đường vòng ghi thẳng prod (chi tiết ở Bước 4 và Phần 2: legacy approve(), auto_approve=true, endpoint -dynamic).
3. Human Gate 1 (2 nút Ghi-temp / Cancel)	Nút đã code đúng spec trong renderWapActions (index.html:1857-1906: "🧪 Duyệt Chạy Thử Trên Staging", "🛑 Huỷ Bỏ & Xoá Staging"); server kiểm tra _require_action → 409 (api.py:239-250); approve_shadow chạy staging + tự gọi Agent 2 audit ngay (api.py:327)	CHƯA ĐẠT trên dashboard / ĐẠT trên chat+REST	Gap chặn đứng: không hề tồn tại element #wap-actions trong DOM (grep wap-actions chỉ trúng 1 dòng JS index.html:1844), nên renderWapActions luôn return sớm ("preview-only mode"). Dashboard không render bất kỳ nút duyệt nào — Gate 1 trên dashboard chết hoàn toàn, chỉ còn Chatlit chat (ui.py:517) và gọi REST tay.
4. Human Gate 2 (A: mời Agent 2 / B: bỏ qua, ghi thẳng prod)	Luồng WAP không có lựa chọn B — publish_prod tự kiểm ready_for_production trong DB (api.py:522-529), chỉ AUDIT_PASSED mới mở; next_actions (incident_store.py:175-179): READY_FOR_PRODUCTION → [publish_prod, cancel_shadow], AUDIT_FAILED_TRIAGE → [replan, manual_override, cancel_shadow] (không có publish)	CHƯA ĐẠT đúng chữ spec (nhưng lệch theo hướng an toàn hơn)	Spec đòi nút B "ghi thẳng prod" — code cố tình không làm (đúng về an toàn, sai về spec). Ngược lại, lựa chọn A cũng không phải nút bấm: audit sau staging là tự động, engineer không được "chọn mời Agent 2". Ngoài ra có 2 biến thể B trá hình nguy hiểm: (a) skip_audit trong chat (ui.py:914-966) gắn nhãn RESOLVED trong khi prod chưa publish gì + không gọi set_status → DB và chat lệch nhau; (b) POST /api/incidents?auto_approve=true (api.py:149-171) và /api/custom-incident (api.py:1514) gọi legacy approve() ghi trực tiếp prod, audit chỉ chạy sau (post-hoc, không phải gate).
5. Agent 2 validation + Loop 1	Agent 2 soi đúng bảng bóng: run_shadow_engine_checks (auditor.py:923-940), diff_shadow_vs_prod, 3 check Cleanliness/Preservation/RowCount; verdict do validator Python suy lại, không tin LLM (schemas.py:909-924); fail → AUDIT_FAILED_TRIAGE + triage alert (index.html renderTriageAlert) + 3 nút replan/sửa-tay/huỷ; replan quay về WAITING_SHADOW_APPROVAL (api.py:426-436)	ĐẠT ~90%	Thiếu đúng 1 câu chữ spec: không có confirm wording "Có muốn gửi yêu cầu này đến Agent 1…?" — nút 🤖 Cho Agent 1 Re-plan (0/1) thực hiện đúng việc đó nhưng không diễn đạt rẽ nhánh Có/Không; nhánh "Không" thể hiện bằng nút Cancel chung, không phải câu hỏi gắn với lỗi Agent 2. Logic đủ, UX thiếu tường minh.
6. Circuit Breaker (fail lần 2 → dừng hẳn)	MAX_REPLAN_ATTEMPTS = 1 (schemas.py:1253); replan_with_feedback trả allowed=False khi hết lượt (agent.py:1750-1756); API trả ok:false không đổi trạng thái (api.py:423-424); UI disable nút replan khi retry_count >= max (index.html:1881-1892); không có đường AUDIT_FAILED_TRIAGE → PUBLISHED (incident_store.py:129-132)	ĐẠT cho vòng tự động	2 kẽ hở: (a) manual_override không giới hạn số lần — engineer có thể lặp manual→approve→audit vô hạn (human-driven nên chấp nhận được, nhưng spec "dừng mọi tiến trình" chưa bao quát); (b) luồng legacy auto_approve hoàn toàn nằm ngoài circuit breaker.
7. Anti-hardcode	Xem Phần 2	CHƯA ĐẠT	Luồng phát hiện (manifest dbt → load_checks, tool_get_table_schema, tool_sample_violations, shadow/atomic theo tên động) là dynamic tốt. Nhưng còn ~10 điểm fix cứng load-bearing (bảng tổng fact_orders, _PROTECTED_TABLES, log tail, playbook mẫu, endpoint demo, fallback schema).
PHẦN 2: ĐIỂM HARDCODE ĐANG TỒN TẠI
Mức NGHIÊM TRỌNG (ghi prod / sai số liệu / crash):

web/backend/api.py:1648-1687 POST /api/publish-prod-dynamic — đường ghi prod không qua gate nào: không _require_action, không kiểm ready_for_production, gọi tool_atomic_publish (bản wrapper không kiểm phase, tools.py:953-985 → wap.atomic_publish cũng không kiểm phase/audit/status, chỉ kiểm schema/exist wap.py:993-1008) trước khi gọi incident_store.publish_to_prod — hàm này không tồn tại (chỉ có set_status/open_incident…), nên request luôn kết thúc bằng AttributeError → trả success:false, nhưng swap bảng đã xảy ra trước đó. Kẻ gọi chỉ cần 1 shadow table tồn tại là tráo được prod, không cần audit, không cần đúng trạng thái. (Hai endpoint -dynamic còn lại cũng gọi hàm không tồn tại: incident_store.approve_shadow ở api.py:1625.)
ai/agent.py:1069 vòng lặp agent chính gọi tools.execute_tool(name, parsed_args) không truyền allowed_tools — LLM nếu hallucinate đúng tên tool_atomic_publish (có mặt trong TOOL_FUNCTIONS, tools.py:1440) sẽ thực thi swap prod mà không cần phase grant (bản _wrap_atomic_publish chỉ đòi grant khi có incident_id, tools.py:1419-1423). Schema LLM không liệt kê tool này (TOOLS_SCHEMA đã lọc, tools.py:1302-1361) nên xác suất thấp, tác động cao — lỗ defense-in-depth.
data/incidents.py:51 SELECT COUNT(*) AS c FROM fact_orders fix cứng làm mẫu số total_rows_scanned/failure_rate cho MỌI incident (kể cả incident ở stg_orders, marts) → báo cáo sai tỉ lệ khi BGK đổi bảng. Đường open_incident (incident_store.py:241) thì đếm đúng theo job.target_table — chỉ đường sample/fallback chat là sai.
Legacy direct-write còn sống: tool_execute_remediation ghi trực tiếp prod (tools.py:415-469), reachable qua agent.approve() (agent.py:1486-1596) ← run_headless(auto_approve=True) (agent.py:1986-1987) ← POST /api/incidents?auto_approve=true (api.py:149-171) và /api/custom-incident (api.py:1514). Ghi chú "dev/test only" nhưng DRA_API_TOKEN mặc định rỗng = API mở, không có gì chặn BGK/kẻ ngoài gọi.
Mức TRUNG BÌNH (sai context / mất bảo vệ khi đổi scenario):

ai/tools.py:196-199 _PROTECTED_TABLES = {fact_orders, dim_customers, dq_test_results, agent_audit_log, dq_baseline_snapshot} — thiếu toàn bộ bảng thật hiện hành (stg_orders, stg_customers, các mart_*) → guard DROP không che chở prod thực tế.
data/dq.py:69-128 FALLBACK_CHECKS fix cứng 6 check trên fact_orders — chỉ kích hoạt khi thiếu dbt manifest, nhưng nếu BGK chạy môi trường không có manifest thì mọi incident đều trỏ nhầm bảng.
data/incidents.py:22-28 PIPELINE_LOG_TAIL chứa FAIL 15 not_null_fact_orders_customer_id — tên test không tồn tại trong CHECKS_BY_NAME; log này từng gây crash on_chat_start (đã thấy trong test.md).
ai/runbooks/dq_playbook.md:16-117 toàn bộ ví dụ vá/quarantine/backfill viết cho fact_orders/customer_id; agent được system prompt dặn đọc playbook trước khi soạn SQL (agent.py:188) → thiên hướng copy mẫu sang case khác (được cứu một phần bởi catalog-schema + preflight + assert_shadow_only).
ai/agent.py:111,135,174,188 system prompt gợi ý đọc runbook lineage_fact_orders + sla_policy — với incident ngoài orders-domain, agent đọc nhầm context downstream (tốn token, sai impact).
web/backend/api.py:901-952 inject-defect: default rows=8; nhánh fallback tự chế SQL INSERT INTO {table} SELECT *, NULL ... (sai số cột) và gọi qua tool_query_duckdb vốn chỉ-đọc → fallback gần như chắc chắn fail. Demo-only nhưng là nút BGK hay bấm ("Case 1").
Mức NHẸ (cosmetic / demo defaults):

index.html:428 "11 / 11 Flows Passing" fix cứng; 795-812 SQL template mẫu stg_orders; 918-926 form simulator default stg_orders / customer_id IS NULL / 8; 1647-1648 fallback text main.stg_orders/customer_id; 1086-1091 BLAST_ITEMS 4 dashboard viết tay; 2474,2498 rows=8.
Debug code lạ trong index.html:1286+ (forceLoadChainlit gán iframe.src='/chat/?t=' — reload cưỡng bức, đi ngược fix lazy-load; testNavigation) — không thuộc flow nghiệp vụ, nên xóa trước demo.
Trả lời câu hỏi BGK đổi bảng/cột/số dòng: luồng phát hiện + investigate + shadow + audit + publish tự thích ứng (đọc manifest/schema/check động). Sẽ gãy/báo sai ở: tỉ lệ failure (điểm 3), guard DROP (điểm 5), fallback khi thiếu manifest (điểm 6), và toàn bộ demo path dùng default stg_orders (điểm 10-11). Sẽ ghi prod trái phép nếu ai chạm vào 3 đường vòng (điểm 1, 2, 4).

PHẦN 3: ĐÁNH GIÁ & ROADMAP (chỉ liệt kê, không sửa)
Điểm sẵn sàng: 6.0/10 cho hackathon (core WAP vững: shadow-guard, preflight, audit độc lập, circuit breaker; trừ điểm nặng vì Gate dashboard chết, còn đường vòng ghi prod, và hardcode mẫu số). Chưa đạt production.

Roadmap ưu tiên (P0 → P2):

P0-1. Vô hiệu hóa hoặc sửa 3 endpoint -dynamic (api.py:1458-1687): hoặc xóa, hoặc bắt đi qua _require_action + ready_for_production + phase grant như endpoint chuẩn. Hiện tại publish-prod-dynamic là lỗ ghi prod không gate.
P0-2. Thêm allowed_tools=AGENT_ALLOWED_TOOLS vào dispatcher ở agent.py:1069, hoặc thêm phase-guard vào _wrap_atomic_publish khi thiếu incident_id.
P0-3. Khôi phục container #wap-actions trên dashboard (hoặc bỏ comment "preview-only") để Gate 1/2/Loop hiển thị được — hiện tại mọi nút duyệt dashboard đều không tồn tại trong DOM.
P0-4. Sửa build_incident_payload (data/incidents.py:51) đếm total theo check.model, không fix fact_orders; sửa PIPELINE_LOG_TAIL dùng tên test tồn tại.
P1-1. skip_audit chat: hoặc publish-then-resolve đúng nghĩa, hoặc đổi nhãn (VD "Tạm đóng, chưa publish") + gọi set_status để DB/chat không lệch.
P1-2. Mở rộng _PROTECTED_TABLES lấy động từ db.list_tables() (trừ shadow_*/quarantine_*) thay vì liệt kê tay 5 bảng cũ.
P1-3. Gate auto_approve=true sau middleware cảnh báo/dev-flag (hoặc yêu cầu token + confirm), vì mặc định API đang mở.
P1-4. Thêm wording rẽ nhánh đúng spec ở Loop 1 ("Gửi lỗi Agent 2 cho Agent 1 điều tra tiếp? [Có/Không]") và nút A "Mời Agent 2 audit" tường minh thay vì auto-audit sau staging (hoặc ghi nhận thiết kế auto-audit vào tài liệu để trả lời BGK).
P2. Dọn debug (forceLoadChainlit, testNavigation), default demo, BLAST_ITEMS, text 11/11; bổ sung runbook lineage generic theo bảng (hoặc dặn agent tra list_runbook_topics trước khi đoán tên).